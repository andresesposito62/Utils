                                                            Sentencias SQL
                                                            
                                                            

*Create Database

    CREATE DATABASE test_db;
    
*Usar por defecto cierta base de datos:

    USE DATABASE test_db;
    
*Crear Tabla: 

    CREATE TABLE `platziblog`.`people` (
      `person_id` INT NOT NULL AUTO_INCREMENT,
      `last_name` VARCHAR(255) NULL,
      `first_name` VARCHAR(255) NULL,
      `city` VARCHAR(255) NULL,
      `address` VARCHAR(255) NULL,
      PRIMARY KEY (`person_id`));
      
 *Insertar Dato:
 
      INSERT INTO `platziblog`.`people` (`person_id`, `last_name`, `first_name`, `city`, `address`) VALUES ('2', 'Esposito', 'karen', 'bogota', ' montecarlo 2');


*Crear View:

    USE `platziblog`;
    CREATE  OR REPLACE VIEW `platzi_people` AS
    SELECT * FROM platziblog.people;

*Modificar tabla:

    ALTER TABLE `platziblog`.`people` 
    ADD COLUMN `date_of_birth` DATETIME NULL AFTER `address`;

    
 
